# Tareas completadas — 2026-08-13

## Telemetría del nivel de razonamiento

- GloryAPI registra por solicitud el esfuerzo pedido (`Bajo`, `Medio`, `Alto` o
  `Máximo`), los tokens de razonamiento y su fuente: `del proveedor`,
  `estimados` o `no confirmado`.
- La lectura acepta los campos directos y las variantes
  `completion_tokens_details.reasoning_tokens` y
  `output_tokens_details.reasoning_tokens`. En streaming se prefiere el chunk
  final de uso del proveedor; la heurística de caracteres solo se usa como
  respaldo visible.
- Analytics muestra el total de tokens de razonamiento y el detalle por
  solicitud. No se guardan prompts, respuestas ni credenciales.
- Rutas principales: `server/src/lib/reasoning-telemetry.ts`,
  `server/src/routes/proxy-log.ts`, `server/src/services/proxy-stream.ts`,
  `server/src/routes/analytics/index.ts` y `client/src/pages/AnalyticsPage.tsx`.
- Evidencia: pruebas dirigidas 15/15 PASS, suite server 52 archivos/291 tests
  PASS, build shared/server/client PASS, migración aditiva verificada en la
  base operativa de 4730 solicitudes y runtime saludable en `:3101`.

### Corrección de verificación real de razonamiento

- CommandCode ahora solicita explícitamente el chunk final de uso durante
  streaming (`stream_options.include_usage`) y mantiene `high` sin degradarlo.
- Un `reasoning_tokens: 0` provisional ya no se presenta como razonamiento del
  proveedor; si hubo deltas de razonamiento se marcan como estimados, y si no
  hay evidencia se muestra `no confirmado`.
- Smoke directo al modelo con `reasoning_effort: high`: HTTP 200, 11 fragmentos
  de razonamiento y 11 tokens reportados por el proveedor.
- Evidencia: suite server 53 archivos/294 tests PASS, build completo PASS y
  GloryAPI reiniciado con el cambio activo.

## Ahorro de tokens y continuidad del Codex Bridge

- Plan: `Agente/planes/completados/plan-ahorro-tokens-bridge-2026-08-12.md`.
- Resultado: se eliminó el duplicado auxiliar de título cuando la firma es
  inequívoca; la auditoría de cierre es compacta y la continuación completa
  solo se usa cuando hace falta.
- Telemetría: GloryAPI registra `request_kind`, relación con el request padre y
  tokens cacheados cuando el proveedor los informa; Analytics lo presenta en
  español.
- Rutas modificadas: las 23 rutas propias enumeradas en el handoff del plan.
- Evidencia: bridge 158/158; `npm test -w server` 50 archivos/286 tests; `npm run build`
  PASS; `git diff --check` PASS.
- Limitación: Sentinel doctor no está listo por `tool-source-missing` de la
  fuente externa `glory-rs-rest`; no se declaró PASS de gate ni se alteró esa
  instalación.

## Recuperación del límite de búsquedas web del bridge

- Resultado: al agotar las rondas web internas, el bridge ejecuta una última
  síntesis sin las herramientas web, usando los resultados ya incorporados al
  historial; si el modelo insiste en pedir web, se devuelve un error recuperable
  bounded en vez de entrar en un ciclo infinito.
- Configuración: `BRIDGE_WEB_TOOL_ROUNDS`, por defecto `3`, acotado entre `1` y
  `10`; las recuperaciones de herramientas mixtas conservan su presupuesto
  independiente.
- Rutas modificadas: `integrations/codex-bridge/bridge/config.js`,
  `integrations/codex-bridge/bridge/upstream-adapter.js`,
  `integrations/codex-bridge/test/browser-stall-regression.test.cjs`,
  `integrations/codex-bridge/test/configuration-contract.test.cjs`,
  `integrations/codex-bridge/README.md` y `roadmap.md`.
- Evidencia: suite bridge `165/165 PASS`; prueba nueva de síntesis al límite
  verifica que la última llamada ya no contiene `web_search` y termina con
  `response.completed`; `git diff --check` PASS antes del commit.

## Ventana uniforme de autocompactación del bridge

- Hallazgo: el home aislado anunciaba `150000` para Auto y algunos proveedores,
  pero `1048576`/`1050000` para CommandCode; además `/v1/models` combinaba una
  ventana por modelo con `auto_compact_token_limit` global de `120000`.
- Resultado: todos los modelos del bridge usan una ventana efectiva y un límite
  de autocompactación de `150000`; el límite interno `CONTEXT_LIMIT_TOKENS`
  también queda en `150000` por defecto. Un valor de catálogo mayor queda
  limitado por el presupuesto global del bridge.
