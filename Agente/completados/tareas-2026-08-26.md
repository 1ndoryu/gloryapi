# Tareas cerradas — 2026-08-26

## NVIDIA NIM — DeepSeek V4 Flash 0731 y rotación de credenciales

**Qué se hizo:** se activó NVIDIA NIM en la configuración operativa y se añadió el modelo
`deepseek-ai/deepseek-v4-flash-0731` con su ruta fijada. El modelo queda explícito y no se añadió a
Auto automáticamente, para no cambiar la cadena existente sin una decisión de enrutamiento separada.

- Proveedor `nvidia`: endpoint OpenAI-compatible de NVIDIA, razonamiento y herramientas declarados,
  contexto físico documentado de 1M y límite operativo del bridge de 150K.
- Modelo: contexto publicado de 150K, razonamiento activo, visión nativa desactivada por contrato de
  entrada de texto del modelo.
- Se guardaron dos credenciales nuevas en la bóveda DPAPI local, etiquetadas como `Cuenta NVIDIA 1`
  y `Cuenta NVIDIA 2`; ambas pasaron la comprobación de salud real.
- La credencial NVIDIA previa (id interno 8), distinta de las dos nuevas, quedó deshabilitada pero no
  eliminada. Así la rotación activa queda limitada exactamente a las dos cuentas solicitadas y la
  credencial anterior permanece recuperable.
- La proyección del bridge se sincronizó desde la configuración canónica. El modelo no ocupa una
  ranura del selector de ChatGPT Desktop porque las seis ranuras visibles existentes ya están asignadas;
  sí queda disponible en GloryAPI y mediante solicitudes explícitas al modelo.

**Evidencia:** `POST /api/health/check/:id` devolvió `healthy` para las credenciales nuevas; una llamada
mínima a `POST /v1/chat/completions` respondió HTTP 200 y `X-Routed-Via: nvidia/deepseek-ai/deepseek-v4-flash-0731`;
la bóveda y los listados no exponen los valores de las claves.

**Fuente externa consultada:** la ficha/API de NVIDIA declara endpoint OpenAI-compatible, modelo
`deepseek-ai/deepseek-v4-flash-0731`, razonamiento y contexto de 1M. El límite de 150K es una decisión
operativa de GloryAPI para la compactación del bridge.

**Pendiente separado:** si se quiere que aparezca también en el selector del bridge, primero hay que
definir una nueva ranura compatible o decidir cuál modelo visible actual debe ocultarse; no se hizo ese
desplazamiento automáticamente.
