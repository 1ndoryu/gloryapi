# Tareas completadas — 2026-08-14

## Ventana de contexto del Desktop bridge

- La causa del indicador `34 mil / 258 mil` era que el catálogo Desktop publicaba identificadores
  reservados/internos (`auto` y `codex-auto-review`) que no todas las versiones ofrecen como opción visible.
  El perfil quedaba sin una entrada seleccionable y Desktop aplicaba su ventana interna de 258400.
- El catálogo ahora publica un alias Desktop para Auto; el bridge normaliza y acepta `auto`
  y `codex-auto-review` como compatibilidad. GloryAPI sigue recibiendo el modelo canónico `auto` y las
  respuestas no exponen el modelo físico de upstream como selector.
- Se reiniciaron solo GloryAPI, el bridge y la ventana con `desktop-user-data-bridge`; la ventana normal
  (PID 3352, sin ese `user-data-dir`) quedó intacta. Las conversaciones no se migran ni se borran.

### Evidencia

- `models.json` aislado: 7 entradas, todas con `context_window`, `max_context_window` y
  `auto_compact_token_limit` en `150000`.
- `GET http://127.0.0.1:4100/health`: `published`, revisión 8, 7 entradas.
- `GET http://127.0.0.1:4100/v1/models`: todas las entradas reportan `150000` en los tres campos.
- Pruebas enfocadas: 36/36 PASS. Suite bridge completa: 174/174 PASS.

## Coherencia del selector, routing y razonamiento del bridge

- Se dejó Enrutamiento con una sola lista de modelos. La pertenencia y el orden
  de `route:auto` se controlan en la fila; la ruta fijada se configura desde la
  misma fila; el modal de Auto conserva únicamente sus opciones generales.
- Se eliminaron del listado principal los indicadores de Inteligencia, Arena,
  AA Coding y Velocidad. El panel conserva solo estado operativo, capacidad,
  visión, alias del selector y controles.
- Se hizo persistente/migrable el alias `pickerId` compatible con Codex Desktop.
  La base real quedó en revisión 8, Auto con miembros `[2, 3]`, seis modelos
  configurados más la entrada Auto y ningún DeepSeek V4 Pro.
- DeepSeek V4 Flash de Andoryyu, OpenCode Zen, OpenCode Go y CommandCode, además
  de Muse de CommandCode, declaran razonamiento. El selector aislado muestra
  `low/high/max` y el traductor transmite `reasoning_effort`; TokenHarbor queda
  desactivado porque su proveedor no declara esa capacidad.
- Se mantuvo el home normal intacto. Se reiniciaron únicamente GloryAPI, el
  bridge y `C:\Users\Owner\.codex-gloryapi\desktop-user-data-bridge`; el
  historial aislado se conserva.

### Evidencia

- `npm run build:server`: PASS.
- `npm run build -w client`: PASS; solo permanece el warning existente del
  chunk grande de Vite.
- `npm test -w server -- --reporter=dot`: 54 archivos / 310 tests PASS.
- `node --test --test-concurrency=1 test/*.test.cjs`: 174 tests PASS.
- `npm run quality:doctor`: PASS, `readyForGate=true`.
- `npm run task:check -- GLORY-COHERENCIA-FULL-20260814F`: PASS, 0 errores;
  quedaron 19 warnings y 3 info preexistentes fuera de este bloque.
- Health del bridge: revisión 8, siete entradas publicadas; el catálogo
  aislado muestra razonamiento para Andoryyu, Zen, Go, CommandCode Flash y
  Muse, y no lo muestra para TokenHarbor.

## Visibilidad de Zen y retorno a Auto en Codex Desktop

- Se confirmó en el catálogo base de Codex Desktop que `gpt-5.6-sol-wm`, la
  ranura asignada a OpenCode Zen, tiene `visibility=hide`. La configuración
  central ahora usa la ranura visible `gpt-5.4-mini`; la migración idempotente
  actualizó la DB operativa a revisión 9 y Zen volvió a aparecer en el picker.
- Codex Desktop retira de la lista el modelo predeterminado. Por eso el menú
  contiene los seis modelos explícitos y Auto se selecciona mediante
  `Avanzado → Restablecer a predeterminado`; el bridge traduce ese estado a la
  ruta canónica `auto`.
- La primera asignación usaba `gpt-5.6-sol` para CommandCode, pero ese es el
  slug que la acción nativa de restablecimiento selecciona. Se corrigió la
  colisión: `gpt-5.6-sol` queda reservado para Auto y CommandCode usa
  `gpt-5.6-auto`.
- `-RefreshConfig` ahora reinicia el bridge antes de regenerar el home aislado,
  evitando que el acceso directo combine una DB nueva con un catálogo cargado
  en memoria de una revisión anterior.

### Evidencia

- `model/list`: siete modelos no ocultos; Zen usa `gpt-5.4-mini`, Auto es el
  predeterminado y los seis explícitos quedan seleccionables.
- `npm run build:server`: PASS; servidor 54 archivos / 310 tests PASS.
- Suite secuencial del bridge: 176/176 PASS; pruebas dirigidas finales 31/31 PASS.
- Health vivo tras corregir el destino nativo de restablecimiento: catálogo
  `published`, revisión 10, siete entradas. ChatGPT normal
  siguió activo en PID 3352 durante el reinicio aislado.
